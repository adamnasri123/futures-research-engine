"""TopstepX / ProjectX API client package.

Modules: auth (session token), accounts, contracts, history (bars), orders,
positions, trades. All REST against api.topstepx.com; credentials from .env
(TOPSTEP_USERNAME, TOPSTEP_API_KEY).
"""
